import type { ReactElement } from 'react';
import React from 'react';
import { GivebackFaq } from './GivebackFaq';

// The FAQ tab. The campaign's "why" now headlines the page hero, so this tab
// focuses purely on the answers.
export const GivebackFaqPanel = (): ReactElement => <GivebackFaq />;
