const blurColors = {
  highlight: 'var(--theme-blur-blur-highlight)',
  baseline: 'var(--theme-blur-blur-baseline)',
  bg: 'var(--theme-blur-blur-bg)',
  glass: 'var(--theme-blur-blur-glass)',
};

export default blurColors;
