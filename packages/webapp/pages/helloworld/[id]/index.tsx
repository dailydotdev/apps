import HelloWorldPage from '../index';

// Re-use the same component but with different params
export default HelloWorldPage;

// Re-use the getServerSideProps logic from the parent page
export { getServerSideProps } from '../index';
