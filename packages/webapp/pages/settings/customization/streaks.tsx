// Streaks settings merged into the combined Streaks & gamification page. This
// route is kept (existing links/bookmarks) and re-exports that single page so
// there are no duplicated toggles across two pages.
export { default } from './gamification';
